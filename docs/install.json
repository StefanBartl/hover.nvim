{
  "tools": [
    {
      "bin": "soffice",
      "required": false,
      "why": "Turns an office document (doc/docx/odt and friends) under the cursor into a picture in the hover window instead of a plain format badge — the conversion runs through pdfport.nvim's soffice producer. On Windows the LibreOffice installer does not extend PATH, so this can report missing on a machine where LibreOffice is plainly installed: add the LibreOffice `program` directory to PATH, or `:Hover office off` to stop asking.",
      "pkg": {
        "apt": "libreoffice",
        "dnf": "libreoffice",
        "pacman": "libreoffice-still",
        "zypper": "libreoffice",
        "apk": "libreoffice",
        "brew": "libreoffice",
        "winget": "TheDocumentFoundation.LibreOffice",
        "scoop": "libreoffice",
        "choco": "libreoffice-fresh"
      }
    },
    {
      "bin": "chrome",
      "required": false,
      "why": "Renders a hovered http(s) link as a picture of the page, instead of previewing it as text — `:Hover links web shot` turns it on, and it is off by default because a render *executes* the page rather than downloading it. Any Chromium-based browser answers: chrome, chromium, brave or msedge, searched in that order. Not on PATH is the normal case rather than the exception — measured on Windows, the Chrome installer does not extend PATH at all, so this reports missing on a machine where Chrome is plainly installed. hover.nvim looks in the usual install locations by itself and will find it there; `links.shot.command` names one outright. Same problem `soffice` above describes.",
      "pkg": {
        "apt": "chromium",
        "dnf": "chromium",
        "pacman": "chromium",
        "zypper": "chromium",
        "apk": "chromium",
        "brew": "--cask google-chrome",
        "winget": "Google.Chrome",
        "scoop": "googlechrome",
        "choco": "googlechrome"
      }
    },
    {
      "bin": "pdftoppm",
      "required": false,
      "why": "Renders the first page of a PDF under the cursor as the hover preview, rather than reporting it as bytes. Ships with poppler and is reached through pdfport.nvim — both have to be present, `:checkhealth hover` says which is missing.",
      "pkg": {
        "apt": "poppler-utils",
        "dnf": "poppler-utils",
        "pacman": "poppler",
        "zypper": "poppler-tools",
        "apk": "poppler-utils",
        "brew": "poppler",
        "winget": "oschwartz10612.Poppler",
        "scoop": "poppler",
        "choco": "poppler"
      }
    },
    {
      "bin": "ffmpeg",
      "required": false,
      "why": "Lifts a still out of a video under the cursor, so a .mp4 shows a frame in the hover window instead of a size in bytes — and the paging keys then step through the file. Reached through media.nvim, which also needs ffprobe from the same install to read the duration a percentage offset is taken of; both come from one package. On Windows, winget and scoop extend the *user* PATH, which a terminal started before the install does not have — media.nvim probes both shim directories itself, and `:checkhealth media` says what it found.",
      "pkg": {
        "apt": "ffmpeg",
        "dnf": "ffmpeg",
        "pacman": "ffmpeg",
        "zypper": "ffmpeg",
        "apk": "ffmpeg",
        "brew": "ffmpeg",
        "winget": "Gyan.FFmpeg",
        "scoop": "ffmpeg",
        "choco": "ffmpeg"
      }
    }
  ]
}
